import media from "./mediaQuery";

const colors = {
  black: "#101820",
  yellow: "#FFD100",
  lightGrey: "#D7D2Cb",
  darkGrey: "#003E51",
  green: "#008578",
  blue: "#007FA3",
};

const fontSizes = {
  small: "14px",
  medium: "16px",
  large: "20px",
};

const font = ["Montserrat, sans-serif"];

const theme = {
  colors,
  media,
  fontSizes,
  font,
};

export default theme;
